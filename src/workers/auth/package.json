{
  "name": "auth-api",
  "version": "1.0.0",
  "description": "Authentication Worker API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
} 